export default function CareerCommonLayout({children}: {children: React.ReactNode}) {
    return (
        <div>
            ths is coming from the main career common layout File
            {children}
        </div>
    )
}